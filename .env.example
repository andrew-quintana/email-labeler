# Copy to .env and fill in. Used locally and by Trigger.dev tasks when running in dev.
# For deployed runs, set the same vars in the Trigger.dev dashboard (Project → Environment Variables).

# =======================
# === REQUIRED CONFIG ===
# =======================

# Trigger.dev and LLM API keys
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_REF=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Gmail OAuth (see docs/SETUP_GMAIL_OAUTH.md). Get refresh token via: pnpm run get-refresh-token
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# ========================
# === OPTIONAL CONFIGS ===
# ========================

# Gmail user; defaults to "me"
# GMAIL_USER_ID=me

# GCP project for setup:gcp-gmail script
# GCP_PROJECT_ID=

# Max messages to trigger per poll (default 5)
# POLL_BATCH_SIZE=5

# Supabase (for processed_emails, prompt_versions, and ML model storage [models bucket]).
# Run supabase/migrations/*.sql in order in the Supabase SQL editor. Without Supabase, pipeline runs in cold start (no recording/training).
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
