# Copy to .env and fill in. Used locally and by Trigger.dev tasks when running in dev.
# For deployed runs, set the same vars in the Trigger.dev dashboard (Project → Environment Variables).

# Required: Trigger.dev and LLMs
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_REF=

# Required: Gmail OAuth (see docs/SETUP_GMAIL_OAUTH.md). Get refresh token via: pnpm run get-refresh-token
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
# Optional; defaults to "me"
# GMAIL_USER_ID=me

# Optional: GCP project ID for setup:gcp-gmail script
# GCP_PROJECT_ID=

# Optional: max messages to trigger per poll (default 5)
# POLL_BATCH_SIZE=5

# Optional: Supabase for processed_emails, prompt_versions, and ML model storage (models bucket).
# Run supabase/migrations/*.sql in order in the Supabase SQL editor, then set these.
# Without Supabase the pipeline runs in cold start (no recording, no trained ML artifacts).
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
